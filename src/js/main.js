/**
 * @type {Object}
 */
const colorjoe = {
  rgb: (dom) => this,
};
/**
 * @type {Object}
 */
const colorWidget = colorjoe.rgb(
  document.querySelector('.color-widget__picker'),
);

colorWidget.show();
